backend = "glx";
vsync = true;

corner-radius = 8;
round-borders = 8;

shadow = false;
fading = false;

blur-method = "none";

use-damage = true;

detect-rounded-corners = true;
detect-client-opacity = true;

unredir-if-possible = true;
